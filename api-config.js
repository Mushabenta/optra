// API base URL for the Optra backend.
// Empty string = same origin, which is correct when the app pages are
// served by the backend itself. Set a full URL (e.g. "https://api.example.com")
// only if the pages are hosted somewhere else.
window.API_BASE_URL = "";
